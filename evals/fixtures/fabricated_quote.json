{
  "short_id": "os_s18_fake_citation",
  "question": "What happens when the MMU reads a page table entry whose valid bit is clear?",
  "beats": [
    {"speaker":"interviewer","line":"What happens when the MMU reads a page table entry whose valid bit is clear?","on_screen":"Valid bit is clear","visual_ref":"title"},
    {"speaker":"student","line":"The MMU raises a page fault trap to the operating system, because the page is not resident in physical memory.","on_screen":"MMU raises a page fault","visual_ref":"d_trap","source_quote":"When the MMU reads a page table entry whose valid bit is clear, it raises a page fault trap to the operating system."},
    {"speaker":"student","line":"The operating system checks the swap priority table and evicts the least recently used frame before reading.","on_screen":"OS evicts an LRU frame","visual_ref":"d_evict","source_quote":"The OS consults the swap priority table and evicts the least recently used frame."},
    {"speaker":"student","line":"It then updates the page table entry and restarts the instruction that faulted, so the process resumes as if nothing happened.","on_screen":"Restart the instruction","visual_ref":"d_restart","source_quote":"updates the page table entry, and restarts the instruction that faulted"},
    {"speaker":"student","line":"The faulting process stays blocked for the whole duration of that disk read, which is why page faults are expensive.","on_screen":"Process blocked on disk I/O","visual_ref":"d_blocked","source_quote":"The faulting process is blocked for the duration of the disk I/O."}
  ]
}
