{
  "short_id": "os_s18_page_fault",
  "question": "What happens when the MMU reads a page table entry whose valid bit is clear?",
  "beats": [
    {
      "speaker": "interviewer",
      "line": "What happens when the MMU reads a page table entry whose valid bit is clear?",
      "on_screen": "Valid bit is clear",
      "visual_ref": "title"
    },
    {
      "speaker": "student",
      "line": "The MMU raises a page fault trap to the operating system, because the page is not in memory.",
      "on_screen": "MMU raises a page fault",
      "visual_ref": "d_trap",
      "source_quote": "When the MMU reads a page table entry whose valid bit is clear, it raises a page fault trap to the operating system."
    },
    {
      "speaker": "student",
      "line": "The operating system finds the page on the backing store, picks a free frame, and issues the read.",
      "on_screen": "OS finds a free frame",
      "visual_ref": "d_frame",
      "source_quote": "The OS locates the page on the backing store, selects a free frame, issues the disk read"
    },
    {
      "speaker": "student",
      "line": "It updates the page table entry and restarts the faulting instruction, so the process resumes.",
      "on_screen": "Restart the instruction",
      "visual_ref": "d_restart",
      "source_quote": "updates the page table entry, and restarts the instruction that faulted"
    }
  ]
}
